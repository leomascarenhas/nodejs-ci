info:
  name: Calculate - Now
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/calculator/add?num1=10&num2=15
  params:
    - name: num1
      value: "10"
      type: query
    - name: num2
      value: "15"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
