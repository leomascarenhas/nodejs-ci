info:
  name: Hello Again
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/helloagain/Leo"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
