info:
  name: Hello
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/hello?name=Lara"
  params:
    - name: name
      value: Lara
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
